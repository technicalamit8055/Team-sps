/**
 * CRM Page - Voter CRM & Ward Analytics Engine
 * 
 * This page provides the intelligent voter management system with:
 * - Infinite scroll pagination
 * - Real-time status sync
 * - Ward-based analytics
 * - Optimistic UI updates
 */
import React from 'react';
import { VoterCRM } from '@/components/VoterCRM';

export const CRM: React.FC = () => {
  return <VoterCRM />;
};
